<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="#F5F5F5">

    <!-- TextView que muestra cuántas notas se tienen -->
    <TextView
        android:id="@+id/tvNotesCount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="📝 Notas guardadas: 0"
        android:textSize="20sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="20dp"
        android:background="@drawable/rounded_background_primary"
        android:textColor="@android:color/white"
        android:padding="16dp"
        android:elevation="4dp" />

    <!-- EditText donde el usuario captura la nota -->
    <EditText
        android:id="@+id/etNewNote"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="✍️ Escribe tu nota aquí..."
        android:textSize="16sp"
        android:minLines="4"
        android:maxLines="6"
        android:gravity="top|start"
        android:layout_marginBottom="20dp"
        android:background="@drawable/rounded_edittext"
        android:padding="16dp"
        android:inputType="textMultiLine|textCapSentences"
        android:elevation="2dp"
        android:textColorHint="#999999" />

    <!-- Botón para guardar la nota -->
    <Button
        android:id="@+id/btnSaveNote"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="💾 Guardar Nota"
        android:textSize="18sp"
        android:layout_marginBottom="20dp"
        android:background="@drawable/rounded_button_primary"
        android:textColor="@android:color/white"
        android:padding="16dp"
        android:elevation="4dp"
        android:textStyle="bold" />

    <!-- TextView multi línea que muestra el listado con las notas -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginBottom="20dp"
        android:background="@drawable/rounded_notes_container"
        android:elevation="2dp">

        <TextView
            android:id="@+id/tvNotesList"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="📋 No hay notas guardadas"
            android:textSize="16sp"
            android:lineSpacingMultiplier="1.3"
            android:padding="20dp"
            android:textColor="#333333"
            android:minHeight="200dp"
            android:gravity="top|start"
            android:fontFamily="sans-serif" />

    </ScrollView>

    <!-- Botón para limpiar las notas -->
    <Button
        android:id="@+id/btnClearNotes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🗑️ Limpiar Todas las Notas"
        android:textSize="16sp"
        android:background="@drawable/rounded_button_danger"
        android:textColor="@android:color/white"
        android:padding="16dp"
        android:elevation="4dp"
        android:textStyle="bold" />

</LinearLayout>
